<h5>Username : <%= @user.username %></h5>
<h5>Currently The Driver of <%= @user.trips.count %> 
	<% if @user.trips.count == 1 %>
		Trip
	<% else %>
		Trips
	<% end %>
</h5>

<%= ratings_for @user, :show_user_rating => true %>
<%= ratings_for @user, :static %>


<p>
<%= link_to "All Trips", trips_path %>
</p>